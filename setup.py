from setuptools import setup, find_packages

setup(
    name = 'worldgame',
    version = '',
    url = '',
    license = 'BSD',
    description = '',
    author = '',
    packages = find_packages('src'),
    package_dir = {'': 'src'},
    install_requires = ['setuptools'],
)
